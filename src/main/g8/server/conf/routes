# PROJECT Routes
# This file defines the Projects application routes (Higher priority routes first)
# ~~~~

# this line is needed so it works for contexts, like /myproject - without you need /myproject/
GET        /                             pme123.adapters.server.boundary.JobCockpitController.index()

# Here are the custom paths. By default none are needed.


->         /webjars                      webjars.Routes
# Reuse the routes from the ADAPTERS project
->         /                             adapters.Routes
